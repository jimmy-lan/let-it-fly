apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-property-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-property-mongo-db
  template:
    metadata:
      labels:
        app: user-property-mongo-db
    spec:
      containers:
        - name: user-property-mongo-db
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: user-property-mongo-service
spec:
  selector:
    app: user-property-mongo-db
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
