apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-property-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-property
  template:
    metadata:
      labels:
        app: user-property
    spec:
      containers:
        - name: user-property
          image: lanyanxi/user-property
          env:
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: JWT_SECRET
            - name: NATS_CLIENT_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: NATS_URL
              value: http://nats-service:4222
            - name: NATS_CLUSTER_ID
              value: letitfly
            - name: MONGO_CONNECTION_URI
              value: "mongodb://user-property-mongo-service:27017/user-property"

---
apiVersion: v1
kind: Service
metadata:
  name: user-property-service
spec:
  selector:
    app: user-property
  ports:
    - name: user-property
      protocol: TCP
      port: 3000
      targetPort: 3000
